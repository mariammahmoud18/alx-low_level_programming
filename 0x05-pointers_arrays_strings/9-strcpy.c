#include "main.h"
#include "string.h"
/**
 * _strcpy - copies a string into another.
 * @des: string to be copied into.
 * @src: string to be copied.
 * Return: pointer to des.
*/

char *_strcpy(char *des, char *src)
{
strcpy(des, src);
return (des);
}
