#include "main.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/**
 * _malloc_checked - duplicate a string.
 * @b: string to be dublicated.
 * Return: pointer.
*/
void *malloc_checked(unsigned int b)
{
int *ptr;
ptr = b;
if (b == NULL)
return (98);
}
